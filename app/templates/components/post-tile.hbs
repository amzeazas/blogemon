{{yield}}

<div class="col-md-4">
  <div class="news">
    <div class="info">
      <h3>{{post.title}}</h3>
      <img src={{post.image}} alt="" height="250px" width="250px" />
      <p>{{post.text}}</p>
    </div>
  </div>
</div>
